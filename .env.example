# --- Required ---
# Full JSON-RPC URL for your target EVM chain (paid endpoint recommended).
# Robinhood Chain public endpoint (rate-limited, fine for a first test):
#   https://rpc.mainnet.chain.robinhood.com
# For real use, paste your dRPC endpoint for Robinhood Chain instead.
EVM_RPC_URL=https://rpc.mainnet.chain.robinhood.com

# --- Strongly recommended ---
# Etherscan/Blockscout-compatible explorer API base for the chain.
# Enables funding-source tracing and contract-source checks.
# Blockscout hosted gateway (reliable; free key from dev.blockscout.com —
# 100k credits/day). The chain's free public instance
# (robinhoodchain.blockscout.com) proved unreliable (5xx/timeouts).
EXPLORER_API_URL=https://api.blockscout.com/4663/api
EXPLORER_API_KEY=
# If the free explorer times out (ReadTimeout in scan notes), try 15 — a slow
# instance may answer given more time; the circuit breaker keeps scans fast
# when it truly doesn't.
EXPLORER_TIMEOUT=6

# --- Optional ---
# Sanity check: fail fast if the RPC serves a different chain.
# Robinhood Chain mainnet is 4663.
EVM_CHAIN_ID=4663

# DexScreener chain slug for scan/pair discovery, if the chain is indexed.
# Robinhood Chain is indexed as "robinhood" (dexscreener.com/robinhood).
DEXSCREENER_CHAIN_ID=robinhood

# Native token symbol, display only.
NATIVE_SYMBOL=ETH

# Free CoinGecko demo API key for the setup scanner's price history
# (src/onchain/setups.py). Without it, GeckoTerminal's keyless tier rate-limits
# hard and sweeps are slow. Get one free at coingecko.com/en/api.
GECKOTERMINAL_API_KEY=

# --- Solana (optional; enables Solana mints in the same quick-scan box) ---
# Solana mainnet JSON-RPC endpoint — dRPC serves it; paste your dashboard URL.
# python -m src.solana.report <mint> works from the CLI too.
SOLANA_RPC_URL=
# Label extra launchpad programs (measured program id shown raw otherwise):
#   SOLANA_VENUES=stonk.fun:<program-id>;another:<program-id>
# The first scan of a token from a launchpad prints the pool's owner program
# id — paste it here to name the venue.
SOLANA_VENUES=
# History paging cap for launch analysis (1000 sigs/page). Deeper history than
# this = launch window reported UNRESOLVED, never guessed.
SOLANA_MAX_SIG_PAGES=10

# X (Twitter) API bearer token for social forensics (src/onchain/social.py).
# Optional. Reading accounts/search requires a paid API tier — check current
# pricing at developer.x.com. Without it, only declared links are checked.
X_BEARER_TOKEN=

# Caps to control RPC spend.
MAX_LOG_BLOCK_RANGE=5000        # blocks per eth_getLogs request
EARLY_WINDOW_SECONDS=60         # "early buyer" = bought within N seconds of pair creation
MAX_EARLY_BUYERS=40             # deep-check at most this many wallets
